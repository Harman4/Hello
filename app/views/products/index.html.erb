<p id="notice"><%= notice %></p>

<h1>Products</h1>
<div class="container-fluid">
  <div class="row">
    <% @products.each do |product| %>
    <div class="col-sm">
      <a href="#"><h1><td><%= product.name %></td></h1></a>
      <p><td><%= product.description %></td></p>
      <td><%= product.image_url %></td>
      <td><%= product.color %></td>
    <td><%= product.price%></td>
    </div><!-- /col -->
    <% end %>
  </div>
    <!-- /col -->
  </div>
</div>

<br>

<%= link_to 'New Product', new_product_path %>
